extends ../layout/index

block variables
  - var title = 'webpack template';
  - var description = 'webpack template';
  - var pageClass = 'index-page';

block content
  ul
    li
      a(href="colors-and-type.html") colors-and-type
  #components 
    example-component

block script 
  script(type="text/javascript"). 
    const foo = true;
    let bar = function() {};
    if (foo) {
      bar(1+5);
    }